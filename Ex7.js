//[M1S05] Ex 7 - Mapeamento de Elementos

const numeros = [3, 1, 2, 4, 7, 5, 9];

const quadrados = numeros.map(numero => numero ** 2);

console.log("Array contendo os quadrados dos números: " + quadrados);